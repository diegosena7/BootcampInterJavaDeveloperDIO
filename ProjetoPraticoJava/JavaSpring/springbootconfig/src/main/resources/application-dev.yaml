#DEV environment

app:
  message=This is the propety file fo the ${spring.application.name} specific to Dev environment

spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:db;DB_CLOSE_DELAY=-1
    username: sa
    password: sa